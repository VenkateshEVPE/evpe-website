import PropTypes from "prop-types";
import clsx from "clsx";

const SectionTitleWithText = ({ spaceTopClass, spaceBottomClass }) => {
  return (
    <div className={clsx("welcome-area", spaceTopClass, spaceBottomClass)}>
      <div className="container">
        <div className="welcome-content text-center">
          <h5>Who Are We</h5>
          <h1>Welcome To EVPE</h1>
          <p>
          evpe streamlines your logistics with custom-built EV fleets, offering comprehensive solutions from financing to fleet management. Designed for hyperlocal and last-mile delivery, we focus on maximizing efficiency and minimizing costs for third-party and commercial logistics operators.{" "}
          </p>
        </div>
      </div>
    </div>
  );
};

SectionTitleWithText.propTypes = {
  spaceBottomClass: PropTypes.string,
  spaceTopClass: PropTypes.string
};

export default SectionTitleWithText;
